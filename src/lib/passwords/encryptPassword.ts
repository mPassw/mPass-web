const encryptPassword = async (password: string, salt: string): Promise<string | null> => {
	try {
		return '';
	} catch (err) {
		console.error(err);
		return null;
	}
};

export { encryptPassword };
